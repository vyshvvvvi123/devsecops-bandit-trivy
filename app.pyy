import subprocess, hashlib, yaml, os
def do_good_things():
# No hardcoded secrets
subprocess.run(["echo", "hello"], check=True) # no shell=True hashlib.sha256(b"abc").hexdigest() # stronger hash yaml.safe_load("a: 1") # safe loader
if name == " main ": do_good_things()
